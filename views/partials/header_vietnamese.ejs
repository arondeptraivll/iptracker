<!DOCTYPE html>
<html lang="vi" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
    
    <!-- SCRIPT HCAPTCHA -->
    <script src="https://js.hcaptcha.com/1/api.js" async defer></script>
    
    <style>
        [role="tablist"] {
            display: flex;
            border-bottom: 1px solid var(--pico-muted-border-color);
            margin-bottom: 1rem;
        }
        [role="tab"] {
            border: none; background: none; padding: 0.75rem 1rem;
            cursor: pointer; border-bottom: 2px solid transparent; margin-bottom: -1px;
        }
        [role="tab"][aria-selected="true"] {
            border-bottom-color: var(--pico-primary);
            color: var(--pico-primary-focus);
        }
        .toast {
            padding: 1rem 1.5rem; border-radius: var(--pico-border-radius);
            margin-bottom: 1.5rem; color: var(--pico-contrast);
            border-left: 5px solid var(--pico-primary);
        }
        .toast.success {
            background-color: hsla(var(--pico-color-green-h), var(--pico-color-green-s), var(--pico-color-green-l), 0.15);
            border-left-color: var(--pico-color-green-500);
        }
        .toast.error {
            background-color: hsla(var(--pico-color-red-h), var(--pico-color-red-s), var(--pico-color-red-l), 0.15);
            border-left-color: var(--pico-color-red-500);
        }
        fieldset[disabled] { opacity: 0.5; pointer-events: none; }
        [data-captcha-disabled] { opacity: 0.6; cursor: not-allowed; pointer-events: none; }
    </style>
</head>
<body>
    <main class="container">
