<!DOCTYPE html>
<html lang="vi" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
    
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://js.hcaptcha.com/1/api.js" async defer></script>
    
    <style>
        /* CSS cũ cho Tab, Fieldset... giữ nguyên */
        [role="tablist"] { display: flex; border-bottom: 1px solid var(--pico-muted-border-color); margin-bottom: 1rem; }
        [role="tab"] { border: none; background: none; padding: 0.75rem 1rem; cursor: pointer; border-bottom: 2px solid transparent; margin-bottom: -1px; }
        [role="tab"][aria-selected="true"] { border-bottom-color: var(--pico-primary); color: var(--pico-primary-focus); }
        fieldset[disabled] { opacity: 0.5; pointer-events: none; }
        [data-captcha-disabled] { opacity: 0.6; cursor: not-allowed; pointer-events: none; }

        /* CSS MỚI CHO SWEETALERT NỀN TỐI */
        body.swal2-shown > [aria-hidden="true"] {
            transition: 0.1s filter;
            filter: blur(5px);
        }
        .swal2-popup.swal2-dark {
            background-color: var(--pico-card-background-color);
            color: var(--pico-card-color);
        }
        /* TÙY CHỈNH NÚT GIỐNG VỚI ẢNH MẪU */
        .swal2-styled.swal2-confirm {
            background-color: #7950f2 !important; /* Màu tím violet */
            border-radius: 8px !important;
        }
        .swal2-styled.swal2-deny, .swal2-styled.swal2-cancel {
            background-color: #fa5252 !important; /* Màu đỏ */
             border-radius: 8px !important;
        }

    </style>
    
    <!-- HÀM HELPER CHUNG ĐƯỢC THAY ĐỔI -->
    <script>
        // Từ toast -> notification lớn ở giữa
        function showSwalNotification(title, text = '', icon = 'success') {
            Swal.fire({
                title: title,
                text: text,
                icon: icon,
                confirmButtonText: 'OK',
                customClass: {
                    popup: 'swal2-dark' // Áp dụng style nền tối
                }
            });
        }
    </script>
</head>
<body>
    <main class="container">
